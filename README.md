# CodeAlpha_BasicNetworkSniffer
A Python Tkinter network intrusion detection GUI that captures live packets with Scapy, classifies protocols, shows Windows-friendly interface names, and saves/loading PCAP/JSON sessions.
